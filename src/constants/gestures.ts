/** Shared thresholds for the horizontal "page between days" swipe. */

/** Horizontal travel before a drag counts as a swipe rather than a tap or a scroll. */
export const SWIPE_SLOP = 20;
/** Either a long enough drag or a quick flick pages. */
export const SWIPE_DISTANCE = 50;
export const SWIPE_VELOCITY = 400;
