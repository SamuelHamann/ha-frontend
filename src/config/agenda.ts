/**
 * Entities backing the Tasks & calendar page. Not secret, so git-tracked.
 *
 * `calendar.home` is Home Assistant's own Local Calendar ("Home 🏡").
 * `todo.inbox` is the Todoist Inbox list.
 * Other options on this instance: calendar.inbox (Todoist as a calendar),
 * todo.home, todo.shopping_list.
 */
export const CALENDAR_ENTITY_ID = 'calendar.home';
export const TODO_ENTITY_ID = 'todo.inbox';
